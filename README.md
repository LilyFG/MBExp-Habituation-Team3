# gamm analysis log:

April 29
- .rmd added (prelim.)
- continue with "re", "fs" computationally implausible
- interactions with tensor est. `te` (a.k.a., "te duur") try do that with hpc (pending...)
  (hopefully a time x age landscape plot that allows for planned contrasts)
- plot_diff CIs: try using Bayesian sim.ci (slow...)
- autocorrections: mostly fine without corrections (thus assume so with `te` without correction efforts)

April 21
- some notes about the vars
- "re" random effect structure used only, not "fs"
- interactions are not yet considered
- autocorrections are not yet corrected
